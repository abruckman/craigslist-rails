<h1><%= @category.name %></h1>
<ul>

<% @posts.each do |post| %>
  <li>
    <%= link_to post.title, category_post_path(@category, post) %>
  </li>
<% end %>
</ul>

<%= link_to 'New Post', new_category_post_path(@category) %> <br />
<%= link_to 'Back', categories_path %>
